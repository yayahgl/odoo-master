{
  "version": 2,
  "service": {
    "name": "odoo-master",
    "icon": "🐘",
    "build": {
      "builder": "DOCKERFILE"
    },
    "deploy": {
      "startCommand": "/usr/bin/python3 /usr/bin/odoo --db_host=${{Postgres.PGHOST}} --db_port=${{Postgres.PGPORT}} --db_user=${{Postgres.PGUSER}} --db_password=${{Postgres.PGPASSWORD}} --database=odoo_master --db_template=False"
    }
  },
  "database": {
    "engine": "postgresql",
    "version": "15"
  }
}